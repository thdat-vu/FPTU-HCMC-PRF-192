//check an integer if it is even or odd
//hàm không đầu vào nhưng trả về.
int kiemTra(int n){
//	int value;
//	value = (n % 2 == 0 ? 1 : 0);
//	return value;
//	return value;
//	return value;
	return (n % 2 == 0);
}